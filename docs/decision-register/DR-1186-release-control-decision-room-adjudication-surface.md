# DR-1186: Release-Control Decision-Room Adjudication Surface

Decision: show the existing assist-language audio decision-snapshot
adjudication binding in the main release-control decision room so teachers and
publishers see one evidence chain. The shared contract rejects cross-tenant
scope and fingerprint tampering before a binding can be described as linked.

The route remains review-only and introduces no release, persistence, approval,
promotion, export, activation, or student-launch action.

Verification: `npm run verify:assist-language-audio-catalog-release-decision-snapshot-binding`.
