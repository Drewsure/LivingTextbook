# Consolidation Plan

## Phase 1 - Preserve the Source Projects

The source projects are imported into `legacy/` for preservation before any architectural choices are made.

## Phase 2 - Discuss the Canonical Architecture

Before promoting code into `apps/web` or `apps/ai-service`, decide the database, auth, service boundary, and curriculum model.
