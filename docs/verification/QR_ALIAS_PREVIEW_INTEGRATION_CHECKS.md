# QR Alias Preview Integration Checks

- `npm run verify:qr-alias-preview-integration`
- `npm run verify:qr-alias-rollback-boundary`
- `npm run verify:foundation-composition`

The preview must consume the shared review-only adapter and must not perform a
redirect or any other route mutation.
