# apps/web

Canonical LivingTextbook web app target.

Initial direction:

- Start from the best architectural pieces of `ministar-lab`.
- Port UI/product surfaces from `ministar-game-studio-ai`.
- Connect to the chosen canonical data model.
- Integrate worksheet/game generation through `apps/ai-service` or equivalent server-side modules.
