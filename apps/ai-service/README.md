# apps/ai-service

Worksheet ingestion, AI analysis, document parsing, and generation service target.

Initial direction:

- Start from the FastAPI and document-processing logic in `ai-quiz-builder-emergent`.
- Preserve PDF, DOCX, text, and image extraction behaviors.
- Provide clean API contracts for `apps/web`.
- Decide later whether this remains a Python service or moves into the main web app.
